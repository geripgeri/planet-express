---
# Install with: ansible-galaxy collection install -r requirements.yaml
collections:
  - name: community.general      # provides community.general.modprobe
    version: ">=12.6.0"
  - name: community.sops         # provides SOPS vars plugin for encrypted host_vars
    version: ">=2.3.0"
