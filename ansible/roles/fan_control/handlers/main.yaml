---
- name: Restart generate-fancontrol
  ansible.builtin.systemd:
    name: "{{ fan_control_generate_service }}"
    state: restarted
    daemon_reload: true

- name: Restart fancontrol
  ansible.builtin.systemd:
    name: "{{ fan_control_service }}"
    state: restarted
