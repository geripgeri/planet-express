[tool.pytest.ini_options]
pythonpath = ["."]
minversion = "7.0"
addopts = "--cov=scripts --cov-report=term-missing --cov-branch"
testpaths = [
    "tests"
]

[tool.coverage.run]
branch = true
source = ["scripts"]

[tool.coverage.report]
show_missing = true
skip_covered = false
